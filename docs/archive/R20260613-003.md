# P002 Review — R20260613-001 (二轮)

> **日期**: 2026-06-13
> **Review Agent**: Claude
> **Review 对象**: Codex 修复提交 a025b7c

---

## 审查结论: FAIL

---

## 问题

### Issue 1: IP 规则归属修复不完整

Codex 仅从 domain/ 删除了 10 个文件（AI.mrs, China.mrs 等），但**未将它们移动到 ip/**。

当前状态：

| 类型 | 配置引用 | 实际文件 | 缺失 | 多余 |
|------|---------|---------|------|------|
| Domain `.mrs` | 19 | 23 | 无 | 4 (Disney, Netflix, Spotify, YouTube) |
| IP `.mrs` | 9 | 10 | 9 | 10 |

**缺失的 IP 文件**（配置引用但不在 ip/）：

```
AIIP.mrs, ChinaIP.mrs, EmbyIP.mrs, GoogleIP.mrs, NetflixIP.mrs,
PrivateIP.mrs, ProxyIP.mrs, SocialMediaIP.mrs, StreamingIP.mrs, TelegramIP.mrs
```

注意：配置中引用的文件名为 `AIIP.mrs`，但实际文件名为 `AI.mrs`。**文件名不匹配**，不是简单移动就能解决。

### Issue 2: 文件名映射关系未处理

配置 `Pro.yaml` 中的 IP 规则文件命名规则为 `<分类>IP.mrs`（如 `AIIP.mrs`），但实际文件名为 `<分类>.mrs`（如 `AI.mrs`）。

可能的解决方向：
1. 重命名实际文件（`AI.mrs` → `AIIP.mrs` 等）
2. 或检查上游源（P003_rules-builder）输出是否应使用 `*IP.mrs` 命名

---

## 修复建议

### Step 1: 确认命名规则

与 P003_rules-builder 确认上游输出的文件命名：
- 如果上游输出 `<分类>.mrs` → 修改 `Pro.yaml` 中的引用
- 如果上游输出 `<分类>IP.mrs` → 修正实际文件名为 `*IP.mrs`

### Step 2: 补齐缺失文件

根据确认结果，补齐所有 IP 规则文件。

### Step 3: 验证

修复后应满足：
- Domain `.mrs` = 配置中非-IP rule-provider 数（19）
- IP `.mrs` = 配置中 IP rule-provider 数（9）
- 文件名与配置 `rule-providers` 中的 URL 完全匹配

---

## 评分

| 检查项 | 结果 |
|--------|------|
| Domain 文件对齐 | PASS |
| IP 文件对齐 | **FAIL** |
| 文件名一致性 | **FAIL** |

---

## Review 优先级

```
FAIL
```

**原因**: IP 规则修复不完整，实际文件从 domain/ 删除但未被移动到 ip/，且文件名与配置引用不匹配（`AI.mrs` vs `AIIP.mrs`）。

---

**归档时间**: 2026-06-13
