# P006: 配置文件批量更新（第二轮）

## 任务目标

对 `mihomo/config/` 三个配置文件执行以下修改：

1. Review FAIL 修复 — ProMax.yaml 规则路由同步遗漏
2. authentication 配置修改
3. allow-lan 配置修改
4. Proxy-Groups 组名调整（广告拦截→Tracking、流媒体→Streaming、货币平台→Crypto、直连→China）
5. ProMax.yaml 新增 Embydirect 组
6. 规则路由排序优化

## 修改范围

### 涉及文件

| 文件 | 角色 |
|---|---|
| `mihomo/config/ProMax.yaml` | 完整版配置（全部修改项） |
| `mihomo/config/Pro.yaml` | 精简版配置（子集修改项） |
| `mihomo/config/Standard.yaml` | 极简版配置（基础修改项） |

### 禁止修改项

- 不修改 `docs/` 目录
- 不修改 `JS/`、`icon/`、`rules/`、`mihomo/openclash/`
- 不修改配置文件中的 proxy-providers URL、DNS 设置、端口、rule-providers URL 等
- 不修改锚点结构（x-* 的名称和顺序不变）
- 不修改 `PROJECT-SPEC.md` 等规范文档

---

## 修改项 1：Review FAIL 修复 — ProMax.yaml 规则路由同步

**文件：** `mihomo/config/ProMax.yaml`

**说明：** 第一轮修改中，proxy-groups 的「国内流量→直连」「漏网之鱼→Final」已在 proxy-groups 段正确重命名，但 rules 段仍有 3 处未同步。

### 修改内容

```yaml
# 改前
- RULE-SET,China,国内流量
- RULE-SET,ChinaIP,国内流量,no-resolve
- MATCH,漏网之鱼

# 改后
- RULE-SET,China,直连  （后续修改项 4 还会改为 China，此处确保与当前 proxy-group 一致）
- RULE-SET,ChinaIP,直连,no-resolve
- MATCH,Final
```

**注意：** 本项为修复性质，后续修改项 4（直连→China）会再次更新目标组名，此处先修正规则引用的组名错误。

---

## 修改项 2：authentication 配置修改

**三个文件**

```yaml
# 改前
authentication: [mihomo:yyds666]

# 改后
authentication: [mihomo:hutao]
```

---

## 修改项 3：allow-lan 配置修改

**三个文件**

```yaml
# 改前
allow-lan: false

# 改后
allow-lan: true
```

---

## 修改项 4：Proxy-Groups 组名调整

### 4.1 ProMax.yaml 组名对照表

| 原组名 | 新组名 | 操作说明 |
|--------|--------|----------|
| 广告拦截 | Tracking | 重命名 group + 同步规则引用 |
| 流媒体 | Streaming | 重命名 group + 同步规则引用 |
| 货币平台 | Crypto | 重命名 group + 同步规则引用 |
| 直连 | China | 重命名 group + 同步锚点引用 + 同步规则引用 |

**保持不变**：Speedtest、Telegram、Socialmedia、Ai、Develop、EMBY、Youtube、Google、Microsoft、Apple、Proxy、Final、故障转移、节点选择、香港节点、台湾节点、日本节点、狮城节点、韩国节点、美国节点，以及所有自动测速/均衡组。

#### 同步要求

修改 `直连→China` 时，以下位置必须全部同步更新：

**锚点引用（x-direct、x-oversea 等列表中无直连/China，无需修改）**

**Proxy-Groups：**
```yaml
# group 定义
- { name: 直连 → China, type: select, proxies: *direct, icon: ... }
```

**Rules 路由目标：**
```yaml
# 国际媒体章节
- RULE-SET,Applecn,直连 → RULE-SET,Applecn,China

# 国内流量章节
- RULE-SET,China,国内流量 → RULE-SET,China,China  （实际值变成了 China,China）
- RULE-SET,ChinaIP,国内流量,no-resolve → RULE-SET,ChinaIP,China,no-resolve
```

#### 广告拦截→Tracking 同步

```yaml
# group
- { name: 广告拦截 → Tracking, type: select, proxies: [REJECT-DROP, REJECT, DIRECT], icon: ... }

# rules（5条）
- RULE-SET,Tracking,广告拦截 → RULE-SET,Tracking,Tracking
- RULE-SET,Advertising,广告拦截 → RULE-SET,Advertising,Tracking
- RULE-SET,AdvertisingIP,广告拦截,no-resolve → RULE-SET,AdvertisingIP,Tracking,no-resolve
- RULE-SET,TrackingIP,广告拦截,no-resolve → RULE-SET,TrackingIP,Tracking,no-resolve
- AND,((DST-PORT,443),(NETWORK,UDP),(NOT,((GEOIP,CN,no-resolve)))),广告拦截 → → AND,((DST-PORT,443),(NETWORK,UDP),(NOT,((GEOIP,CN,no-resolve)))),Tracking
```

#### 流媒体→Streaming 同步

```yaml
# group
- { name: 流媒体 → Streaming, ... }

# rules
- RULE-SET,Streaming,流媒体 → RULE-SET,Streaming,Streaming
- RULE-SET,StreamingIP,流媒体,no-resolve → RULE-SET,StreamingIP,Streaming,no-resolve
```

#### 货币平台→Crypto 同步

```yaml
# group
- { name: 货币平台 → Crypto, ... }

# rules
- RULE-SET,Crypto,货币平台 → RULE-SET,Crypto,Crypto
```

### 4.2 Pro.yaml 组名对照表

| 原组名 | 新组名 |
|--------|--------|
| 流媒体 | Streaming |
| 直连 | China |

**同步要求：**

```yaml
# group
- { name: 流媒体 → Streaming, ... }
- { name: 直连 → China, ... }

# rules
- RULE-SET,Streaming,流媒体 → RULE-SET,Streaming,Streaming
- RULE-SET,StreamingIP,流媒体,no-resolve → RULE-SET,StreamingIP,Streaming,no-resolve
- RULE-SET,China,直连 → RULE-SET,China,China
- RULE-SET,ChinaIP,直连,no-resolve → RULE-SET,ChinaIP,China,no-resolve
```

### 4.3 Standard.yaml 组名对照表

| 原组名 | 新组名 |
|--------|--------|
| 直连 | China |

```yaml
# group
- { name: 直连 → China, ... }

# rules
- RULE-SET,China,直连 → RULE-SET,China,China
- RULE-SET,ChinaIP,直连,no-resolve → RULE-SET,ChinaIP,China,no-resolve
```

---

## 修改项 5：ProMax.yaml 新增 Embydirect 组

在 proxy-groups 区域（EMBY 组附近）新增：

```yaml
- { name: Embydirect, type: select, proxies: [DIRECT, *us-first], icon: https://github.com/Koolson/Qure/raw/master/IconSet/Color/Direct.png }
```

规则同步：将 `Embydirect,DIRECT` 改为 `Embydirect,Embydirect`：

```yaml
# 改前
- RULE-SET,Embydirect,DIRECT

# 改后
- RULE-SET,Embydirect,Embydirect
```

---

## 修改项 6：规则路由排序优化

对规则进行重新排序，按优先级排列。**注意：** 排序时需以修改项 4 和 5 之后的最终组名为准。

### 6.1 ProMax.yaml 规则最终顺序

```yaml
rules:
  # 广告拦截
  - RULE-SET,Tracking,Tracking
  - RULE-SET,Advertising,Tracking
  - RULE-SET,AdvertisingIP,Tracking,no-resolve
  - RULE-SET,TrackingIP,Tracking,no-resolve
  - AND,((DST-PORT,443),(NETWORK,UDP),(NOT,((GEOIP,CN,no-resolve)))),Tracking

  # 本地/私有
  - RULE-SET,Private,DIRECT
  - RULE-SET,PrivateIP,DIRECT,no-resolve

  # 网络测试
  - RULE-SET,Speedtest,Speedtest

  # 即时通讯
  - RULE-SET,Telegram,Telegram
  - RULE-SET,TelegramIP,Telegram,no-resolve

  # 社交平台
  - RULE-SET,Socialmedia,Socialmedia
  - RULE-SET,SocialmediaIP,Socialmedia,no-resolve

  # 人工智能
  - RULE-SET,Ai,Ai
  - RULE-SET,AiIP,Ai,no-resolve

  # 开发服务
  - RULE-SET,Dev,Develop

  # Youtube
  - RULE-SET,YouTube,Youtube
  - RULE-SET,YoutubeIP,Youtube,no-resolve

  # 流媒体
  - RULE-SET,Streaming,Streaming
  - RULE-SET,StreamingIP,Streaming,no-resolve

  # Apple 国内服务
  - RULE-SET,Applecn,China

  # EMBY
  - RULE-SET,Embydirect,Embydirect
  - RULE-SET,Emby,EMBY

  # 货币平台
  - RULE-SET,Crypto,Crypto

  # 谷歌服务
  - RULE-SET,Google,Google
  - RULE-SET,GoogleIP,Google,no-resolve
  - RULE-SET,Microsoft,Microsoft
  - RULE-SET,Apple,Apple
  - RULE-SET,AppleIP,Apple,no-resolve

  # 国外流量
  - RULE-SET,Proxy,Proxy
  - RULE-SET,ProxyIP,Proxy,no-resolve

  # 国内流量
  - RULE-SET,China,China
  - RULE-SET,ChinaIP,China,no-resolve

  # 兜底规则
  - MATCH,Final
```

### 6.2 Pro.yaml 规则最终顺序

```yaml
rules:
  # 本地/私有
  - RULE-SET,Private,DIRECT
  - RULE-SET,PrivateIP,DIRECT,no-resolve

  # 即时通讯
  - RULE-SET,TM,Telegram
  - RULE-SET,Telegram,Telegram
  - RULE-SET,TelegramIP,Telegram,no-resolve

  # 社交平台
  - RULE-SET,SocialMedia,Socialmedia
  - RULE-SET,SocialMediaIP,Socialmedia,no-resolve

  # 人工智能
  - RULE-SET,AI,Ai
  - RULE-SET,AiIP,Ai,no-resolve

  # 开发服务
  - RULE-SET,Dev,Develop

  # 流媒体
  - RULE-SET,Streaming,Streaming
  - RULE-SET,StreamingIP,Streaming,no-resolve

  # Apple
  - RULE-SET,Apple,Apple

  # Youtube
  - RULE-SET,YouTube,Youtube
  - RULE-SET,YoutubeIP,Youtube,no-resolve

  # 国外流量
  - RULE-SET,Proxy,Proxy
  - RULE-SET,ProxyIP,Proxy,no-resolve

  # 国内流量
  - RULE-SET,China,China
  - RULE-SET,ChinaIP,China,no-resolve

  # 兜底规则
  - MATCH,Final
```

### 6.3 Standard.yaml 规则最终顺序

```yaml
rules:
  # 本地/私有
  - RULE-SET,Private,DIRECT
  - RULE-SET,PrivateIP,DIRECT,no-resolve

  # 国外流量
  - RULE-SET,Proxy,Proxy
  - RULE-SET,ProxyIP,Proxy,no-resolve

  # 国内流量
  - RULE-SET,China,China
  - RULE-SET,ChinaIP,China,no-resolve

  # 兜底规则
  - MATCH,Final
```

---

## 执行顺序建议

1. 先处理 ProMax.yaml（改动最多，最复杂）
2. 再处理 Pro.yaml（子集改动）
3. 最后处理 Standard.yaml（最简改动）

---

## 验收标准

1. `authentication: [mihomo:hutao]` — 三个文件全部正确
2. `allow-lan: true` — 三个文件全部正确
3. ProMax.yaml 组名重命名全部正确（4 项），规则引用同步无误
4. Pro.yaml 组名重命名全部正确（2 项），规则引用同步无误
5. Standard.yaml 组名重命名全部正确（1 项），规则引用同步无误
6. ProMax.yaml 新增 Embydirect 组，同步规则引用
7. 三个文件的规则排序与最终顺序一致
8. 第一轮 FAIL 的 3 条 ProMax.yaml 规则已修复
9. 所有文件中无悬挂引用（引用的策略组名必须存在于 proxy-groups 中）
10. YAML 格式正确，加载无报错

## 风险说明

- 组名变更需同步 proxy-groups、anchors（如适用）和 rules 三处
- 直连在中国地区的规则引用（Applecn,Embydirect,China,MATCH）需要全部排查
- 排序后有逻辑变更即重新生成整个 rules 段，注意不要遗漏任何规则
- 建议完成修改后用 `grep` 核对每个文件中的策略组名是否统一
