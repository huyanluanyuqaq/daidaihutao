# P010: ProMax.yaml 与 Pro.yaml 配置同步及规则排序统一

## 任务目标

1. ProMax.yaml：修正 Embydirect 策略组 proxies，统一 rules 分组排序
2. Pro.yaml：精简 proxy-groups，rules 按 ProMax 结构排序
3. AGENTS.md / CLAUDE.md 阶段一工作流已由 Claude 直接更新（不在本任务执行范围内）

## 修改范围

| 文件 | 操作 |
|---|---|
| `mihomo/config/ProMax.yaml` | proxy-groups 修改 + rules 重排 |
| `mihomo/config/Pro.yaml` | proxy-groups 删除 + rules 重排 |
| `mihomo/config/Standard.yaml` | 不修改 |

## 禁止修改项

- 不修改 `Standard.yaml`
- 不修改 `mihomo/openclash/` 下的 `.conf` 文件
- 不修改锚点别名定义（`*oversea`, `*us-first`, `*direct`, `*sg-first` 等）
- 不修改 `docs/`、`PROJECT_LESSONS.md`、`PROJECT-SPEC.md`、`AGENTS.md`、`CLAUDE.md`
- 不修改规则集提供者（`rule-providers`）的 URL 或格式

---

## 变更 1：ProMax.yaml — Embydirect 策略组

**位置**：`proxy-groups` → Embydirect

| 字段 | 当前值 | 目标值 |
|---|---|---|
| `proxies` | `[DIRECT, *us-first]` | `*direct` |

**操作**：将 `{ name: Embydirect, type: select, proxies: [DIRECT, *us-first], ... }` 改为 `{ name: Embydirect, type: select, proxies: *direct, ... }`。保留其他字段不变。

---

## 变更 2：ProMax.yaml — rules 重排

将 rules 段从当前分组重新排序为以下 10 组标准结构。组间用空行分隔。

### 目标排序

```yaml
  # 1. 本地/私有
  - RULE-SET,Private,DIRECT
  - RULE-SET,PrivateIP,DIRECT,no-resolve

  # 2. 广告拦截
  - RULE-SET,Tracking,Tracking
  - RULE-SET,Advertising,Tracking
  - RULE-SET,AdvertisingIP,Tracking,no-resolve
  - RULE-SET,TrackingIP,Tracking,no-resolve

  # 3. 高优先级特定服务
  - RULE-SET,Ai,Ai
  - RULE-SET,AiIP,Ai,no-resolve
  - RULE-SET,Telegram,Telegram
  - RULE-SET,TelegramIP,Telegram,no-resolve

  # 4. 专属流媒体与社交
  - RULE-SET,Youtube,Youtube
  - RULE-SET,YoutubeIP,Youtube,no-resolve
  - RULE-SET,Streaming,Streaming
  - RULE-SET,StreamingIP,Streaming,no-resolve
  - RULE-SET,Embydirect,Embydirect
  - RULE-SET,Emby,EMBY
  - RULE-SET,Socialmedia,Socialmedia
  - RULE-SET,SocialmediaIP,Socialmedia,no-resolve

  # 5. 特殊垂直领域
  - RULE-SET,Crypto,Crypto
  - RULE-SET,Dev,Develop
  - RULE-SET,Speedtest,Speedtest

  # 6. 大厂特定服务
  - RULE-SET,Google,Google
  - RULE-SET,GoogleIP,Google,no-resolve
  - RULE-SET,Applecn,China
  - RULE-SET,Apple,Apple
  - RULE-SET,AppleIP,Apple,no-resolve
  - RULE-SET,Microsoft,Microsoft

  # 7. QUIC / UDP 443 管控
  - AND,((DST-PORT,443),(NETWORK,UDP),(NOT,((GEOIP,CN,no-resolve)))),Tracking

  # 8. 通用国外流量
  - RULE-SET,Proxy,Proxy
  - RULE-SET,ProxyIP,Proxy,no-resolve

  # 9. 通用国内流量
  - RULE-SET,China,China
  - RULE-SET,ChinaIP,China,no-resolve

  # 10. 兜底规则
  - MATCH,Final
```

### 关键变化说明

- `AND,((DST-PORT,443)...)` 从组 2（广告拦截）末尾移动到独立的组 7
- `Embydirect/Emby` 从独立段归入组 4（流媒体与社交）
- `Applecn` 从独立段归入组 6（大厂）
- `Crypto` 从独立段归入组 5（特殊垂直领域）

**规则内容一条不增、一条不减**，仅重新排序和分组注释。

---

## 变更 3：Pro.yaml — proxy-groups 精简

**位置**：`proxy-groups` → 路由策略区域（`# 路由策略` 注释下的策略组）

### 当前策略组（路由策略区域）

```yaml
  # 路由策略
  - { name: Telegram, ... }
  - { name: Socialmedia, ... }
  - { name: Ai, ... }
  - { name: Develop, ... }
  - { name: Streaming, ... }
  - { name: Apple, ... }
  - { name: Youtube, ... }   # ← 需删除
  - { name: Proxy, ... }
  - { name: China, ... }
  - { name: Final, ... }
```

### 保留策略组

只保留以下 9 个路由策略组，**删除 Youtube 策略组**：

```
Telegram, Socialmedia, Ai, Develop, Streaming, Apple, Proxy, China, Final
```

**注意**：`# 自动选择`、`# 区域测速` 等基础设施组（故障转移、节点选择、香港节点等）**保留不动**，它们是锚点引用的基础设施。

---

## 变更 4：Pro.yaml — rules 重排

按 ProMax 的 10 组标准结构排序，Pro.yaml 目前包含以下规则：

- Private, PrivateIP
- TM, Telegram, TelegramIP
- SocialMedia, SocialMediaIP
- AI, AiIP
- Dev
- YouTube, YoutubeIP
- Streaming, StreamingIP
- Apple
- Proxy, ProxyIP
- China, ChinaIP
- MATCH

### 目标排序

```yaml
  # 1. 本地/私有
  - RULE-SET,Private,DIRECT
  - RULE-SET,PrivateIP,DIRECT,no-resolve

  # 2. 高优先级特定服务
  - RULE-SET,TM,Telegram
  - RULE-SET,Telegram,Telegram
  - RULE-SET,TelegramIP,Telegram,no-resolve
  - RULE-SET,AI,Ai
  - RULE-SET,AiIP,Ai,no-resolve
  - RULE-SET,Dev,Develop

  # 3. 专属流媒体与社交
  - RULE-SET,YouTube,Youtube
  - RULE-SET,YoutubeIP,Youtube,no-resolve
  - RULE-SET,Streaming,Streaming
  - RULE-SET,StreamingIP,Streaming,no-resolve
  - RULE-SET,SocialMedia,Socialmedia
  - RULE-SET,SocialMediaIP,Socialmedia,no-resolve

  # 4. 大厂特定服务
  - RULE-SET,Apple,Apple

  # 5. 通用国外流量
  - RULE-SET,Proxy,Proxy
  - RULE-SET,ProxyIP,Proxy,no-resolve

  # 6. 通用国内流量
  - RULE-SET,China,China
  - RULE-SET,ChinaIP,China,no-resolve

  # 7. 兜底规则
  - MATCH,Final
```

### 关键变化说明

- `AI/AiIP/Dev` 与原 `TM/Telegram/TelegramIP` 合并入组 2（高优先级特定服务）
- `YouTube/YoutubeIP` 从独立段归入组 3（流媒体与社交）
- `SocialMedia/SocialMediaIP` 从独立段归入组 3（流媒体与社交）
- 组编号 #1~#7，跳过 ProMax 中不存在的组

**规则内容一条不增、一条不减**，仅重新排序和分组注释。

Pro.yaml 的 `rule-providers` 段**无需变动**。

---

## 验收标准

1. ProMax.yaml Embydirect 的 `proxies` 为 `*direct`
2. ProMax.yaml rules 按变更 2 目标顺序排列，共 10 组
3. ProMax.yaml rules 条数变更前后一致（不增不减）
4. Pro.yaml proxy-groups 中无 Youtube 策略组
5. Pro.yaml rules 按变更 4 目标顺序排列
6. Pro.yaml rules 条数变更前后一致（不增不减）
7. 两个文件 YAML 语法正确
8. Standard.yaml 内容完全不变

## 测试要求

变更完成后执行：

```bash
# YAML 语法验证
python -c "import yaml; yaml.safe_load(open('mihomo/config/ProMax.yaml','r',encoding='utf-8')); yaml.safe_load(open('mihomo/config/Pro.yaml','r',encoding='utf-8')); yaml.safe_load(open('mihomo/config/Standard.yaml','r',encoding='utf-8')); print('All YAML valid')"

# rules 条数校验（变更前后应一致）
echo "ProMax RULE-SET + AND + MATCH:" && grep -cE 'RULE-SET|AND|MATCH' mihomo/config/ProMax.yaml
echo "Pro RULE-SET + MATCH:" && grep -cE 'RULE-SET|MATCH' mihomo/config/Pro.yaml
echo "Standard RULE-SET + MATCH:" && grep -cE 'RULE-SET|MATCH' mihomo/config/Standard.yaml
```

## 风险说明

- ProMax.yaml AND QUIC 规则从组 2 移至独立组 7，匹配顺序变化——QUIC 流量不再在广告拦截前匹配
- Pro.yaml 删除 Youtube 策略组，Youtube 流量将走对应的 `*us-first` 区域锚点策略组
- 纯排序和分组变更，不影响现有 `rule-providers` 配置

## 执行 Agent

Codex（配置修复、规则路由调整）