# P026: 统一修改 Mihomo 配置参数

## 变更内容

对 `mihomo/config/` 下三个配置文件进行统一修改：

### 1. Standard.yaml

| 操作 | 配置项 | 旧值 | 新值 |
|---|---|---|---|
| 注释 | `proxy-providers.Backup` | 启用 | 注释掉 |
| 修改 | `log-level` | `silent` | `info` |
| 修改 | `external-controller` | `127.0.0.1:9090` | `0.0.0.0:9090` |
| 修改 | `tun.auto-redirect` | `true` | `false` |
| 修改 | `dns.listen` | `7874` | `0.0.0.0:7874` |

### 2. Pro.yaml

| 操作 | 配置项 | 旧值 | 新值 |
|---|---|---|---|
| 注释 | `proxy-providers.Backup` | 启用 | 注释掉 |
| 修改 | `log-level` | `silent` | `info` |
| 修改 | `external-controller` | `127.0.0.1:9090` | `0.0.0.0:9090` |
| 修改 | `tun.auto-redirect` | `true` | `false` |
| 修改 | `dns.listen` | `7874` | `0.0.0.0:7874` |

### 3. ProMax.yaml

| 操作 | 配置项 | 旧值 | 新值 |
|---|---|---|---|
| 注释 | `proxy-providers.Backup` | 启用 | 注释掉 |
| 注释 | `proxy-providers.Landing` | 启用 | 注释掉 |
| 修改 | `log-level` | `silent` | `info` |
| 修改 | `external-controller` | `127.0.0.1:9090` | `0.0.0.0:9090` |
| 修改 | `tun.auto-redirect` | `true` | `false` |
| 修改 | `dns.listen` | `7874` | `0.0.0.0:7874` |

## 修改文件

- `mihomo/config/Standard.yaml`
- `mihomo/config/Pro.yaml`
- `mihomo/config/ProMax.yaml`

## 验证方式

1. 确认三个 YAML 文件语法正确（可用 `yamllint` 或加载到 Mihomo 检查）
2. 确认 `git diff` 仅包含上述变更，无多余修改