# 任务 P017：新增 PayPal 策略组及同步 hutaorules 仓库新规则

## 描述

扫描 hutaorules 仓库 `rules` 分支中的 `.mrs` 规则文件，与当前配置文件对比，提取新增规则添加到 `rule-providers` 中。同时新建 PayPal 策略组（us-first 路由）。

## 分析结果

### hutaorules 仓库现有规则（相对于 ProMax.yaml 的新增项）

| 新增规则 | 类型 | 说明 | 路由至现有策略组 |
|---|---|---|---|
| Paypal | domain | PayPal 支付 | 新建 Paypal 组（us-first） |
| Ai-cn | domain | 中国大陆 AI 服务 | China（直连） |
| Cloudflare | domain | Cloudflare 服务 | Proxy（通用国外） |
| Googlefcm | domain | Google Firebase Cloud Messaging | Google（复用 Google 组） |
| Public-tracker | domain | BT 公共 Tracker | Proxy（通用国外） |

所有 ipcidr 类规则已在 ProMax.yaml 中，无新增。

## 修改内容

### 文件 1: `mihomo/config/ProMax.yaml`

#### 1a. proxy-groups — 新增 Paypal 策略组

在 `# 5. 特殊垂直领域` 区域，Crypto 之后添加（新增行用 `+` 标记）：

```yaml
   # 5. 特殊垂直领域
   - { name: Crypto, type: select, proxies: *jp-first, icon: https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/Cryptocurrency_3.png }
+  - { name: Paypal, type: select, proxies: *us-first, icon: https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/PayPal.png }
   - { name: Develop, type: select, proxies: *us-first, icon: https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/GitHub.png }
```

#### 1b. rules — 新增规则路由

在 `# 5. 特殊垂直领域` 区域，`RULE-SET,Crypto,Crypto` 之后添加：

```yaml
+  - RULE-SET,Paypal,Paypal
+  - RULE-SET,Ai-cn,China
```

在 `# 6. 大厂特定服务` 区域，`RULE-SET,Microsoft,Microsoft` 之后添加：

```yaml
+  - RULE-SET,Cloudflare,Proxy
+  - RULE-SET,Googlefcm,Google
```

在 `# 8. 通用国外流量` 区域，`RULE-SET,Proxy,Proxy` 之后添加：

```yaml
+  - RULE-SET,Public-tracker,Proxy
```

#### 1c. rule-providers — 新增规则集

在 rule-providers 区域末尾（`YoutubeIP` 之后）添加：

```yaml
+  Paypal:         { <<: *rule-set-domain, url: https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/Paypal.mrs, proxy: China }
+  Ai-cn:          { <<: *rule-set-domain, url: https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/Ai-cn.mrs, proxy: China }
+  Cloudflare:     { <<: *rule-set-domain, url: https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/Cloudflare.mrs, proxy: China }
+  Googlefcm:      { <<: *rule-set-domain, url: https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/Googlefcm.mrs, proxy: China }
+  Public-tracker: { <<: *rule-set-domain, url: https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/Public-tracker.mrs, proxy: China }
```

#### 1d. fake-ip-filter — 新增直连 rule-set 引用

Ai-cn 指向 China（直连），需加入 fake-ip-filter：
- 原值：
  ```yaml
  fake-ip-filter: [+.miwifi.com, +.docker.io, +.market.xiaomi.com, +.push.apple.com, 'rule-set:Private', 'rule-set:Applecn', 'rule-set:Embydirect', 'rule-set:China']
  ```
- 目标值：
  ```yaml
  fake-ip-filter: [+.miwifi.com, +.docker.io, +.market.xiaomi.com, +.push.apple.com, 'rule-set:Private', 'rule-set:Applecn', 'rule-set:Embydirect', 'rule-set:China', 'rule-set:Ai-cn']
  ```

### 文件 2: `mihomo/config/Pro.yaml`

#### 2a. proxy-groups — 新增 Paypal 策略组

在 Apple 之后添加：

```yaml
+  - { name: Paypal, type: select, proxies: *us-first, icon: https://cdn.jsdelivr.net/gh/Koolson/Qure@master/IconSet/Color/PayPal.png }
```

#### 2b. rules — 新增 Paypal 规则

在 Apple 规则之后添加：

```yaml
+  - RULE-SET,Paypal,Paypal
```

#### 2c. rule-providers — 新增 Paypal 规则集

在 rule-providers 末尾添加：

```yaml
+  Paypal:   { <<: *rule-set-domain, url: https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/Paypal.mrs, proxy: China }
```

### 文件 3: `mihomo/config/Standard.yaml`

无变更。

## 变更总结

| 文件 | 新增规则 | 新增策略组 | 新增 rule-providers |
|---|---|---|---|
| ProMax.yaml | Paypal、Ai-cn、Cloudflare、Googlefcm、Public-tracker | Paypal（us-first） | Paypal、Ai-cn、Cloudflare、Googlefcm、Public-tracker |
| Pro.yaml | Paypal | Paypal（us-first） | Paypal |
| Standard.yaml | — | — | — |

## 注意事项

1. 只有 Paypal 需要新建策略组；其他规则（Ai-cn → China、Cloudflare → Proxy、Googlefcm → Google、Public-tracker → Proxy）复用现有策略组
2. 新增的 rule-providers URL 使用 hutaorules 仓库路径：`https://raw.githubusercontent.com/huyanluanyuqaq/hutaorules/refs/heads/rules/mihomo/domain/XXXX.mrs`
3. 直连规则（Ai-cn → China）需加入 fake-ip-filter 避免被 fake-ip 拦截
4. 所有新增 rule-providers 设置 `proxy: China` 通过国内代理拉取，与现有规则集一致
5. Paypal 图标使用 Qure 已有 `PayPal.png`（注意大小写：`PayPal` 非 `Paypal`）
