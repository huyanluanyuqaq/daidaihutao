# PROJECT_LESSONS.md

## P002: daidaihutao 经验教训

---

### P002 项目中停顿等待确认

**日期：** 2026-06-12
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 错误
**关键词：** 继续执行, 框架流程, 自动化

#### 发生了什么

在 P002 项目文件拉取和初始提交过程中，多次出现"无法给到相关内容"的停顿，用户持续回复"继续"推动进度。实际上应直接执行下一步。

#### 影响或价值

用户需要用"继续"来推进，说明自动化意识不够。框架已明确"修改前先简要说明计划，然后直接执行"。

#### 正确做法 / 后续复用方式

- 计划说完后直接执行，不要停
- 批量操作完成后自动推进到下一阶段
- 除非有重大歧义或高风险，否则不需要等确认

---

### docs/inbox/ 误提交到 Claude 侧

**日期：** 2026-06-12
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 错误
**关键词：** 文档传递, docs/inbox, Claude侧, Codex侧, .gitignore

#### 发生了什么

将 `docs/inbox/` 目录（应为 Codex 侧本地使用）错误地提交到了 Claude 侧的 `claude/review` 分支。

#### 影响或价值

违反了框架级文档传递协议。

#### 正确做法

- Claude 侧：`docs/claude-review.md` → `git push` → `claude/review`
- Claude 侧：`.gitignore` 只忽略 `docs/inbox/`
- Codex 侧：从 `docs/` 读取规范文档，将修复指令写入本地 `docs/inbox/`

---

### 规范文档命名应为 claude-review.md

**日期：** 2026-06-12
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 错误
**关键词：** 文档命名, claude-review.md, 规范文档

#### 发生了什么

使用 `docs/inbox/repair-manual.md` 作为规范文档，未遵循约定命名。

#### 影响或价值

规范文档命名不统一。

#### 正确做法

规范文档统一命名为 `docs/claude-review.md`，推送到 `claude/review` 分支。

---

### 规则文件与配置不一致

**日期：** 2026-06-12
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 规则
**关键词：** 规则文件, rule-providers, .mrs, 配置对齐

#### 发生了什么

`rules/` 目录中 `Pro.yaml` 声明的 33 个 rule-provider 与本地 `.mrs` 文件严重不一致：缺失 15 个、多余 26 个、4 个归属错误。

#### 影响或价值

配置引用不存在的规则文件会导致 mihomo 启动报错。

#### 正确做法

以配置文件的 `rule-providers` 声明为唯一权威来源，Codex 按 `docs/claude-review.md` 执行修复。P003_rules-builder 接入后由外部仓库管理规则文件。

---

### 第一轮完成：架构重构 + Codex 修复 + 复盘沉淀

**日期：** 2026-06-12
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 优点
**关键词：** 复盘沉淀, CLAUDE_LESSONS, CLAUDE.md, 规则统一

#### 发生了什么

第一轮完成后，将总结写入 `docs/round1-summary.md`，同时将经验教训沉淀到 `CLAUDE_LESSONS.md`（新增 3 条），并将项目信息更新到 `CLAUDE.md`。遵循了框架规则要求的"每轮总结写入复盘文件→沉淀→更新框架规则"完整流程。

#### 影响或价值

确保了经验教训不会被遗忘，新项目可以直接参考已沉淀的规则。Codex 成功执行了修复任务（删除多余文件），验证了 Claude/Codex 双 AI 协同模式的可行性。

#### 正确做法 / 后续复用方式

1. 每轮任务完成后立即更新 `PROJECT_LESSONS.md`
2. 经验教训分类：错误 / 优点 / 提示词 / 规则
3. 将值得长期保留的规则提炼到 `CLAUDE.md`
4. 生成项目级总结报告 `docs/round1-summary.md`

---

### 第一轮完成 + Codex 修复审查 + 合并到 main

**日期：** 2026-06-12
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 优点
**关键词：** 修复审查, 两轮审查, 缺失文件, P003待补充

#### 发生了什么

Codex 完成 `codex/automate` 分支修复后，Claude 进行第一轮审查。审查发现：删除任务正确完成（70+ 个多余文件已清理），但补充缺失文件的 10 个 `*IP.mrs` 未完成——这些文件上游 `666OS/rules` 不存在，需等 P003_rules-builder 生成。审查通过后合并到 `main`。

#### 影响或价值

验证了两轮审查机制的有效性：第一轮 Codex 执行，第二轮 Claude 审查。Codex 完成了它能做的全部（删除多余），缺失文件明确标记为 P003 待补充，没有盲目处理。

#### 正确做法 / 后续复用方式

1. Codex 执行完成后，Claude 审查应逐文件验证（domain/ip/根目录分别统计对比）
2. 缺失文件分两类：上游不存在的 → 标记 P003 补充；上游存在的 → 让 Codex 补充
3. 审查通过后合并 `codex/automate → main`，不创建中间总结文件到 docs/

---

### 推送前需同步 README 策略组计数

**日期：** 2026-06-21
**项目编号：** P002
**项目名称：** daidaihutao
**类型：** 规则
**关键词：** README, 策略组计数, 推送前审查, 一致性

#### 发生了什么

P016 阶段三 Review 的推送前一致性审查发现，README.md 中 ProMax.yaml 和 Pro.yaml 的策略组计数（17 / 9）与实际严重不符（实际为 28 / 18）。该数据在上游项目 P015 及更早的配置变更中未被同步更新，长期存在过期问题。

#### 影响或价值

README 中的计数误导用户了解配置复杂度，也反映推送前审查未执行到位。

#### 正确做法 / 后续复用方式

1. 每次合并到 main 后的推送前审查必须检查 README 的策略组计数与实际一致。
2. 可考虑在 PROJECT-SPEC.md 中增加自动检查提醒。
3. 策略组增减在任务文档中标记 "需更新 README 计数"。
